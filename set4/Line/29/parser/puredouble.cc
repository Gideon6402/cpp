#include "parser.ih"

bool Parser::pureDouble(double *dest, string const &str)
{
    // all character must be used thus no white spaces:
    for (C_iter iter = str.cbegin(); iter != str.cend(); ++iter)
    {
        
    }

    // execute stod.

    // set d_integral to false
    // otherwise to true
    return true;
}