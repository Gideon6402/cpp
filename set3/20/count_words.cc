#include "main.ih"

void count_words()
{
//     int n_words = 0;
//     while (true)
//     {
//         if (cin >> word)               // correct syntax?
//             n_words += 1;
//         if (cin.fail())
//             break;
    // }

//     cout << n_words;
}

// // assume cin << word will always take the characters between whitespaces from input stream
// can I use argv[1] multiple times, would it be better?