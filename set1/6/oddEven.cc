# include <iostream>
# include <stdio.h>

using namespace std;

int main()
{
    size_t value;
    // cin >> value;

    value = ~0;

    // cout << (value%2 == 0 ? "even" : "odd") << endl;
    // cout << ( (value & 1) == 1 ? "odd" : "even") << endl;
    cout << value << endl;


    return 0;
}