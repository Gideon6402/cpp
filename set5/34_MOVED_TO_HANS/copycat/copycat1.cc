#include "copycat.ih"

CopyCat::CopyCat(int argc, char **argv)
{
    // structure:
    // <type of copy> <name of copy> = <duplication function>(<original>, <size of original>)

    // initialize:
    // initialize d_data
    // initialize d_size
}

// return by return value is better method because always initialize.